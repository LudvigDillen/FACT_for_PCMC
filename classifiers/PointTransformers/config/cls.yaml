batch_size: 20  # 27
epoch: 75  # 500
learning_rate: 0.0005
gpu: 0
optimizer: Adam
weight_decay: 0.0001
normal: true
model:
  nneighbor: 16
  nblocks: 4
  transformer_dim: 128  # TODO: Investigate if I could decrease this ... Should save lots of memory
  name: Hengshuang
# Which features to use (we always use xyz)
features_to_create:
  use_label: true  # class label
  use_jde: true  # joint differential entropy
  use_sde: true  # separate differential entropy
  use_wd: true  # wasserstein distance
  use_c: false # covisibility weight
  use_s: false  # static weight
  use_cj: true  # joint cardinality ratio
  use_cs: true  # separate cardinality ratio
features_to_use:
  use_label: true  # class label
  use_jde: true  # joint differential entropy
  use_sde: true  # separate differential entropy
  use_wd: true  # wasserstein distance
  use_c: false # covisibility weight
  use_s: false  # static weight
  use_cj: true  # joint cardinality ratio
  use_cs: true  # separate cardinality ratio
batch_size_feature_extraction: 256
# Data setup
num_point: 1024  # 512
re_use_data: false
n_scenes: 300
n_samples_per_scene: 10
dataset: 'v1.0-trainval'  # 'v1.0-trainval'
# Other settings
debug: false
ablation: false




# batch_size: 16  # 16
# epoch: 500  # 200
# learning_rate: 0.0005  # 1e-3
# gpu: 0  # 1
# num_point: 1024
# optimizer: Adam
# weight_decay: 1e-4
# normal: True

# defaults:
#   - model: Hengshuang

# hydra:
#   run:
#     dir: log/cls/${model.name}

#   sweep:
#     dir: log/cls
#     subdir: ${model.name}
