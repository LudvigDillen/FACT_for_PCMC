# Create developing environment
conda create -n PCAC python=3.9.12
conda activate PCAC
pip install autopep8
pip3 install torch torchvision torchaudio
pip install "git+https://github.com/facebookresearch/pytorch3d.git" # no using I think

# Not using currently
# pip install numba required sudo access ... 
# You needed in that case to install CUDA-SDK https://developer.nvidia.com/cuda-downloads 
# Therefore, I used conda instead
conda install numba
conda update numba
conda install cudatoolkit


# Test cupy
pip install cupy-cuda11x # Not using currently


# Get data
wget https://www.nuscenes.org/data/v1.0-mini.tgz # replace with wanted files
tar -xf v1.0-mini.tgz
pip install nuscenes-devkit &> /dev/null

# Parameter tuning
pip install ax-platform


# For visualization
pip install open3d
pip install plotly

